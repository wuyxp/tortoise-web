name: "Deploy (dual GitHub Pages + Aliyun nginx)"

# 议题: MEETING-2026-05-11-06-docs-isolation
# 架构: tortoise-web (PUBLIC) submodule 拉 wuyxp/tortoise-docs (PUBLIC)
#       └─ tortoise-docs 由主仓 scripts/sync-public-docs.sh 同步 PRIVATE 主仓 docs/ 公开子集
# 双发: 国外用户走 GitHub Pages, 国内用户走阿里云 nginx (任一失败不阻塞另一边)

on:
  push:
    branches: [main]
  workflow_dispatch:
  # 主仓 docs-dispatch.yml 跑 sync 脚本 + push tortoise-docs 后触发本 workflow
  repository_dispatch:
    types: [docs-updated]

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Astro build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout tortoise-web
        uses: actions/checkout@v4

      - name: Checkout tortoise-docs (PUBLIC) into src/content/tortoise-source
        # tortoise-docs 是 PUBLIC 仓, actions/checkout 跨仓 public 不需要 PAT.
        uses: actions/checkout@v4
        with:
          repository: wuyxp/tortoise-docs
          path: src/content/tortoise-source
          ref: main

      - name: Verify docs content
        run: |
          test -f src/content/tortoise-source/README.md
          test -f src/content/tortoise-source/SYNC_INFO.md
          echo "✓ tortoise-docs submodule 拉到 ($(find src/content/tortoise-source -name '*.md' | wc -l) .md)"

      - name: Lint cross-repo docs links (P1-4)
        run: bash scripts/check-docs-links.sh

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build
        env:
          PUBLIC_SITE_URL: ${{ secrets.PUBLIC_SITE_URL || 'https://serpilo.com' }}
          PUBLIC_SITE_NAME: ${{ secrets.PUBLIC_SITE_NAME || 'Tortoise' }}
          PUBLIC_FEEDBACK_EMAIL: ${{ secrets.PUBLIC_FEEDBACK_EMAIL || 'tortoise@example.com' }}
          PUBLIC_PLAY_STORE_URL: ${{ secrets.PUBLIC_PLAY_STORE_URL || '#' }}
          PUBLIC_DOMESTIC_STORE_URL: ${{ secrets.PUBLIC_DOMESTIC_STORE_URL || '#' }}
          PUBLIC_GITHUB_REPO: ${{ secrets.PUBLIC_GITHUB_REPO || 'https://github.com/wuyxp/kotlin-stop-shortTV' }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  deploy-pages:
    name: Deploy to GitHub Pages (国外用户)
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    continue-on-error: true
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4

  deploy-aliyun:
    name: Deploy to Aliyun nginx (国内用户)
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 5
    continue-on-error: true
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Rsync dist to Aliyun nginx
        # 注: GitHub Actions 不允许 if 条件直接访问 secrets (security 限制).
        # secret 缺失时 step 会 fail, continue-on-error: true 不阻塞 deploy-pages.
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.ALIYUN_HOST }}
          username: ${{ secrets.ALIYUN_SSH_USER }}
          key: ${{ secrets.ALIYUN_SSH_KEY }}
          source: "dist/*"
          target: ${{ secrets.ALIYUN_DEPLOY_PATH }}
          strip_components: 1
          rm: true
